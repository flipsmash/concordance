# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/

# Frontend (webapp/frontend)
node_modules/

# Model weights — large, fetched separately (see README)
models/
*.gguf

# Book inputs (copyrighted) and all generated data — keep the public repo code-only
*.epub
*.pdf
*.csv
archive/
incoming/
gutenberg_literature/
*.zip
missed_shadow.txt

# Local agent / editor config
.claude/

# Secrets / local env
.env
*.env

# Database dumps (data, not code)
*.dump
*.sql
db_backups/

# Licensed WordNet Domains / SentiWords data — do not publish
wordnet-domains-sentiwords/

# Wiktextract dump (large, fetched separately — see concordance/wiktextract.py)
# and generated pronunciation audio (large, regenerable via `concordance audio`)
data/
audio/

# Operational logs from one-off scripts (scripts/*.sh) — output, not source
scripts/*.log
